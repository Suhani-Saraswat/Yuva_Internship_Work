const app=document.querySelector('#app');const nav=document.querySelector('#nav');const menu=document.querySelector('#menuBtn');const pages={home:{title:'Build better habits with <span>TaskFlow.</span>',text:'A simple single page application that updates content without full page reloads.'},features:{title:'Useful features for your workflow.',text:'Hash-based routing, dynamic content and responsive design are included.'},about:{title:'About this SPA',text:'This project demonstrates client-side routing and DOM manipulation using plain JavaScript.'}};function render(){const route=location.hash.slice(1)||'home';const page=pages[route];if(!page){app.innerHTML='<section><h1>404</h1><p>Page not found.</p><a class="button" href="#home">Return Home</a></section>';return}app.innerHTML='<section class="hero"><div class="wrap"><p class="tag">WEEK 5 • SINGLE PAGE APPLICATION</p><h1>'+page.title+'</h1><p>'+page.text+'</p><a class="button" href="#features">View Features</a></div></section><section class="soft"><div class="heading"><h2>Dynamic content</h2><p>The current section was rendered using JavaScript.</p></div><div class="cards"><article class="card"><h3>Client-side Routing</h3><p>URL hash changes update the content without a full reload.</p></article><article class="card"><h3>Reusable Layout</h3><p>A shared HTML shell keeps the application organized.</p></article><article class="card"><h3>Responsive Design</h3><p>CSS media queries adapt the layout to smaller screens.</p></article></div></section>'}menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')});addEventListener('hashchange',render);addEventListener('DOMContentLoaded',render);